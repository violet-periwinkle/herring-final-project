<!DOCTYPE html>
<html>
  <head>
    <title>Final Project</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-sm navbar-light bg-light justify-content-between">
      <div class="navbar-nav mr-auto">
        <%= link_to "palettes", palettes_path, class: "nav-item nav-link" %>
        <%= link_to "users", users_path, class: "nav-item nav-link" %>
      </div>
      <div class="nav navbar-nav navbar-right ml-auto">
        <% if !session[:current_user] %>
          <%= link_to "login", signin_path, class: "nav-item btn btn-outline-primary mx-1" %>
        <% else %>
          <%= link_to "logout", signin_path, method: :delete, data: { turbo_method: :delete }, class: "nav-item btn btn-outline-primary mx-1" %>
        <% end %>
        
      </div>
    </nav>
    <div class="container-fluid d-flex flex-column min-vh-100">
    <%= yield %>
    </div>
  </body>
</html>
